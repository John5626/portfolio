#include <stdio.h>
#include <stdlib.h>
#include "lista_dp_enc.h"

void percorrer_inverso(lista_dp* L){
    if(!vazia_dp(L)){
        celula* atual = (celula*) (sizeof(celula*));

        atual = L.
    }
}